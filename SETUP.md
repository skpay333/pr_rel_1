# CryptoPay Mini - Полная инструкция по настройке и запуску

## 📱 Что есть в проекте

### ✅ Готовые компоненты:

1. **Фронтенд (React + TypeScript)**
   - Дашборд с балансом USDT
   - Страница пополнения (TopUp) с QR-кодом
   - Страница оплаты (Pay)
   - История транзакций
   - Поддержка (Support)
   - Настройки (Settings)
   - Neo-Brutalist дизайн (жирные границы, яркие цвета, тени)
   - Полностью на русском языке

2. **Бэкенд (Express.js + TypeScript)**
   - REST API для всех операций
   - PostgreSQL база данных
   - Обработка депозитов USDT (TRC20)
   - Система уникальных сумм для автоматической идентификации платежей
   - Блокчейн-сканер для автоматического подтверждения депозитов
   - WebSocket для real-time уведомлений
   - Курс USD/RUB с ExchangeRate-API

3. **Админ панель**
   - Просмотр всех заявок на пополнение
   - Обработка платежей
   - Изменение сумм
   - Просмотр чеков
   - Комментарии оператора

4. **База данных (PostgreSQL)**
   - Таблицы: users, deposits, payment_requests, notifications, operators
   - Все USDT суммы с точностью 8 знаков (как в TRON)
   - Миграции через Drizzle ORM

### 🔧 Технические особенности:

- **USDT Precision**: Все суммы хранятся и обрабатываются с точностью 8 знаков
- **BigInt арифметика**: Предотвращает ошибки с floating-point
- **Centralized TRON config**: Все настройки TRON в одном месте
- **Безопасность**: Helmet, CORS, rate limiting
- **Telegram WebApp SDK**: Интеграция с Telegram

---

## 🌐 Текущие URL доступа

### Development (для разработки):
```
https://919d685a-68de-4bff-80c6-1b5823814ddc-00-3cfscgar2c10x.spock.replit.dev
```

**⚠️ Эта ссылка только для тестирования!** Не используйте её в боте для пользователей.

### После публикации (Production):
После нажатия кнопки "Publish" в Replit вы получите постоянную ссылку типа:
```
https://cryptopay-mini--skpay1.repl.co
```
Эту ссылку нужно будет использовать в Telegram боте.

---

## 📋 Что нужно сделать ПЕРЕД ЗАПУСКОМ

### 1️⃣ Создать и настроить Telegram бота

**Шаги:**

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username)
4. Получите **BOT_TOKEN** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)
5. Сохраните этот токен

**Настройка Mini App:**

1. Отправьте BotFather команду `/mybots`
2. Выберите вашего бота
3. Нажмите "Bot Settings" → "Menu Button"
4. Выберите "Configure menu button"
5. Введите URL вашего опубликованного приложения:
   ```
   https://ваш-домен.repl.co
   ```
6. Введите текст кнопки (например: "💳 Открыть кошелёк")

### 2️⃣ Настроить переменные окружения в Replit

**Обязательные переменные:**

```bash
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_PASSWORD=ваш_секретный_пароль_123
```

**Опциональные (рекомендуется для production):**

```bash
TRONGRID_API_KEY=ваш_ключ_от_trongrid
```

**Как добавить:**
1. Откройте вкладку "Secrets" (🔒) в левом меню Replit
2. Нажмите "New Secret"
3. Введите название переменной и значение
4. Нажмите "Add Secret"

### 3️⃣ Получить TronGrid API ключ (опционально, но рекомендуется)

1. Зайдите на [TronGrid](https://www.trongrid.io/)
2. Зарегистрируйтесь
3. Создайте API ключ
4. Добавьте в Secrets как `TRONGRID_API_KEY`

**Зачем:** Без ключа API имеет лимиты запросов. С ключом - больше requests/sec для blockchain scanner.

### 4️⃣ Настроить TRON мастер-кошелёк

**Текущий адрес для пополнений:**
```
THVyqrSDMBvpibitvTt4xJFWxVgY61acLu
```

**⚠️ ВАЖНО:** Это тестовый адрес! Перед запуском в production:

1. Создайте свой TRON кошелёк (например, в TronLink)
2. Откройте файл `server/config/tron.ts`
3. Замените `MASTER_WALLET_ADDRESS` на ваш адрес
4. Сохраните приватный ключ в безопасном месте (НЕ в коде!)

### 5️⃣ Опубликовать приложение (Deploy/Publish)

1. Нажмите кнопку **"Deploy"** или **"Publish"** в правом верхнем углу Replit
2. Выберите тип: **"VM"** (Virtual Machine)
   - ✅ VM нужен для WebSocket и blockchain scanner
3. Подтвердите публикацию
4. Получите постоянный URL
5. Скопируйте этот URL в настройки бота (см. шаг 1️⃣)

---

## 🚪 Как зайти во все панели

### 1. Пользовательский интерфейс (для пользователей)

**Доступ через Telegram:**
1. Откройте вашего бота в Telegram
2. Нажмите кнопку Menu (или `/start`)
3. Нажмите кнопку "💳 Открыть кошелёк" (или как вы назвали)
4. Откроется Mini App с дашбордом

**Прямой доступ (для тестирования):**
```
https://ваш-домен.repl.co/
```

**Функции:**
- Просмотр баланса USDT и RUB
- Пополнение через QR-код
- Создание заявок на выплату
- История транзакций
- Уведомления

### 2. Админ панель (для операторов)

**URL:**
```
https://ваш-домен.repl.co/admin
```

**Логин:**
- Пароль: тот, что вы установили в `ADMIN_PASSWORD`

**Функции:**
- Список всех заявок на пополнение
- Фильтр по статусам (pending, confirmed, rejected)
- Обработка платежей:
  - Изменение суммы
  - Добавление комментария
  - Просмотр чеков
  - Подтверждение/отклонение
- Real-time обновления

### 3. API документация

**Base URL:**
```
https://ваш-домен.repl.co/api
```

**Основные эндпоинты:**

#### Пользователи:
```
GET  /api/users/profile/:telegramId
POST /api/users/create
```

#### Депозиты:
```
POST /api/deposits/create-automated  - Создать депозит
GET  /api/deposits/user/:userId      - Депозиты пользователя
POST /api/deposits/confirm           - Подтвердить депозит (admin)
```

#### Платежи:
```
POST /api/payment-requests           - Создать заявку на выплату
GET  /api/payment-requests/user/:id  - Заявки пользователя
GET  /api/payment-requests/all       - Все заявки (admin)
```

#### Уведомления:
```
GET /api/notifications/:userId       - Уведомления пользователя
```

#### Курс валют:
```
GET /api/exchange-rates              - Текущий курс USD/RUB
```

---

## 🔍 Что работает, а что НЕТ

### ✅ Что РАБОТАЕТ:

1. **Создание депозитов** с уникальными суммами (8 знаков)
2. **База данных** PostgreSQL с правильной точностью
3. **API** для всех операций
4. **Админ панель** для обработки заявок
5. **Фронтенд** с Neo-Brutalist дизайном
6. **Курс USD/RUB** (автообновление каждые 30 минут)
7. **WebSocket** для real-time уведомлений
8. **Точность USDT**: Все операции с 8 знаками после запятой

### ⚠️ Что НЕ настроено (нужно сделать):

1. **BOT_TOKEN** - бот не подключен
2. **ADMIN_PASSWORD** - админ панель без защиты
3. **TRONGRID_API_KEY** - blockchain scanner работает с лимитами
4. **Production URL** - приложение не опубликовано
5. **Мастер кошелёк** - используется тестовый адрес

### 🚧 Что НЕ работает без настройки:

1. **Telegram авторизация** - без BOT_TOKEN не работает
2. **Блокчейн сканер** - без API ключа медленная работа
3. **Автоматическое подтверждение депозитов** - без blockchain scanner
4. **Бот уведомления** - без BOT_TOKEN

---

## 📝 Чек-лист перед запуском в PRODUCTION

### Обязательно:

- [ ] Создать Telegram бота через @BotFather
- [ ] Добавить `BOT_TOKEN` в Secrets
- [ ] Добавить `ADMIN_PASSWORD` в Secrets
- [ ] Заменить тестовый TRON адрес на свой
- [ ] Опубликовать приложение (Deploy → VM)
- [ ] Настроить Menu Button в боте с URL приложения
- [ ] Протестировать создание депозита
- [ ] Протестировать админ панель
- [ ] Проверить WebSocket подключение

### Рекомендуется:

- [ ] Получить `TRONGRID_API_KEY` для лучшей производительности
- [ ] Настроить мониторинг (логи, ошибки)
- [ ] Протестировать с реальными USDT (маленькие суммы!)
- [ ] Создать резервную копию базы данных
- [ ] Настроить алерты для критических ошибок

### Опционально:

- [ ] Подключить свой домен вместо `.repl.co`
- [ ] Настроить SSL сертификат (автоматически в Replit)
- [ ] Добавить аналитику (Google Analytics, Amplitude)
- [ ] Настроить backup базы данных

---

## 🆘 Поддержка и документация

### Файлы документации:

- `replit.md` - Техническая документация проекта
- `design_guidelines.md` - Руководство по дизайну
- `SETUP.md` - Эта инструкция

### Структура проекта:

```
├── client/           # React фронтенд
├── server/           # Express бэкенд
│   ├── config/       # Конфигурация (TRON, etc)
│   ├── controllers/  # API контроллеры
│   ├── services/     # Бизнес-логика
│   └── storage.ts    # Работа с БД
├── shared/           # Общие типы и схемы
└── components.json   # Конфигурация UI компонентов
```

### База данных:

**Подключение:**
```
DATABASE_URL=postgresql://postgres:password@helium/heliumdb?sslmode=disable
```

**Миграции:**
```bash
npm run db:push        # Синхронизация схемы
npm run db:studio      # Открыть Drizzle Studio
```

---

## 🔐 Безопасность

### ⚠️ НИКОГДА не коммитьте в Git:

- `BOT_TOKEN`
- `ADMIN_PASSWORD`
- `TRONGRID_API_KEY`
- Приватные ключи TRON кошельков
- DATABASE_URL с реальными credentials

### ✅ Используйте Replit Secrets для:

- Всех паролей
- API ключей
- Токенов
- Sensitive данных

---

## 📞 Следующие шаги

1. **Сейчас сделайте:**
   - Создайте бота через @BotFather
   - Добавьте `BOT_TOKEN` и `ADMIN_PASSWORD` в Secrets
   - Опубликуйте приложение (Deploy)

2. **Потом:**
   - Протестируйте все функции
   - Замените тестовый TRON кошелёк
   - Получите TronGrid API ключ

3. **Перед запуском пользователям:**
   - Проверьте все чеклисты выше
   - Протестируйте с реальными (маленькими!) суммами
   - Убедитесь что blockchain scanner работает

---

**Готово! 🚀** Проект настроен и ждёт конфигурации перед запуском.
